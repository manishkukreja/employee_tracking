<!DOCTYPE html>
<html>
<head>
  <title>Test3</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

	<% if user_signed_in? %>
		 <p>Hello <%= current_user.username %> <br />
		 <%= link_to 'Sign out', destroy_user_session_path, method: :delete %></p>
		 <%= link_to "HOME" ,:root unless params[:controller].eql?("users") && params[:action].eql?("index") %>
	<%- else %>
		 <p>
		 <%= link_to 'Register', new_user_registration_path %> |
		 <%= link_to 'ADMIN Sign in', new_user_session_path %>
		 </p>
	<% end %>


<body>

<%= yield %>

</body>
</html>
